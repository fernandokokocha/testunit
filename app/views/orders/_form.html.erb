<%= form_for(@order) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :quantity %><br>
    <%= f.number_field :quantity %>
  </div>
  <div class="field">
    <%= f.label :paid %><br>
    <%= f.check_box :paid %>
  </div>
  <div class="field">
    <%= f.label :sent %><br>
    <%= f.check_box :sent %>
  </div>
  <div class="field">
    <%= f.label :product %><br>
    <%= f.collection_select :product_id, @products, :id, :name %>
  </div>
  <div class="field">
    <%= f.label :customer %><br>
    <%= f.collection_select :customer_id, @customers, :id, :to_s %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
